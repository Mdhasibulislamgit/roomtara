<nav class="navbar navbar-expand-md bg-body-tertiary border-bottom sticky-top">
  <div class="container">
    <a class="navbar-brand d-flex align-items-center" href="/">
      <i class="fa-regular fa-compass me-2"></i>
      <span class="brand-text">RoomTara</span>
    </a>
    
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <!-- Search Bar -->
      <form class="d-flex mx-auto search-form" action="/listings/search" method="GET">
        <div class="input-group">
          <input
            class="form-control search-input"
            type="search"
            name="query"
            placeholder="Search for places..."
            aria-label="Search"
          >
          <button class="btn btn-search" type="submit">
            <i class="fas fa-search"></i>
          </button>
        </div>
      </form>

      <!-- Navigation Links -->
      <div class="navbar-nav ms-auto">
        <a class="nav-link <%= active === 'home' ? 'active' : '' %>" href="/">
          <i class="fas fa-home"></i> Home
        </a>
        <a class="nav-link <%= active === 'listings' ? 'active' : '' %>" href="/listings">
          <i class="fas fa-list"></i> All Listings
        </a>
        <a class="nav-link <%= active === 'new' ? 'active' : '' %>" href="/listings/new">
          <i class="fas fa-plus"></i> Add Room
        </a>
      </div>
    </div>
  </div>
</nav>

<style>
.navbar {
  padding: 0.8rem 0;
  background-color: white !important;
}

.brand-text {
  font-weight: 600;
  font-size: 1.3rem;
  color: #fe424d;
}

.search-form {
  width: 40%;
}

.search-input {
  border-radius: 25px 0 0 25px !important;
  border: 1px solid #dee2e6;
  padding-left: 1.5rem;
}

.btn-search {
  border-radius: 0 25px 25px 0 !important;
  background-color: #fe424d;
  color: white;
  border: 1px solid #fe424d;
}

.btn-search:hover {
  background-color: #e63946;
  border-color: #e63946;
  color: white;
}

.nav-link i {
  margin-right: 0.5rem;
}

@media (max-width: 768px) {
  .search-form {
    width: 100%;
    margin: 1rem 0;
  }
  
  .navbar-nav {
    margin-top: 0.5rem;
  }
}
</style>
